//constantes
let initialData = {
  loggedIn: false
}
let LOGIN = 'LOGIN'
//reducer
export default function (state = initialData, action){
  switch(action.type){
    case LOGIN:

    default:
      return state
  }
}
//action (action creator)